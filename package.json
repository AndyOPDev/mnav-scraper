{
  "name": "mnav-scraper",
  "version": "1.0.1",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install": "npm install --omit=optional --no-audit --no-fund"
  },
  "dependencies": {
    "puppeteer": "21.11.0",
    "express": "4.18.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
