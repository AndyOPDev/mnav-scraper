{
  "name": "mnav-scraper-ultimate",
  "version": "3.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "preinstall": "apt-get update && apt-get install -y chromium-browser || true",
    "postinstall": "ln -s /usr/bin/chromium-browser /usr/bin/chromium || true"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer-core": "^21.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
